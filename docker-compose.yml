services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tailwindui-protocol
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - '3000:3000'
