import { HeroPattern } from '@/components/HeroPattern'

export const metadata = {
  priority: 0,
  title: 'API Documentation',
  description:
    'Learn everything there is to know about the Protocol API and integrate Protocol into your product.',
  descriptionMd:
    'Learn everything there is to know about the Protocol API and integrate Protocol into your product.',
  summary: `This page is a documentation style template for a fictional Protocol API. It introduces the API at a high level and positions it as the way to access contacts, conversations, group messages, and other resources. The layout is built around a hero pattern header, call to action buttons, and a section list that defines top level navigation categories like Guides and Resources. The content is written in a product documentation tone, encouraging developers to create an application in developer settings, read HTTP API docs or client SDKs, and publish integrations to a directory once ready. Buttons link to a quickstart and SDK exploration, mirroring a typical API landing page. While the text is short, it establishes the structure and styling used for larger docs, including lead paragraph styling and a not prose callout area. This page appears to be a sample or test document used to validate documentation components rather than a full set of API instructions. It provides a clear skeleton for future expansion with additional guides, endpoints, and examples.`,
}

export const sections = [
  { title: 'Guides', id: 'guides' },
  { title: 'Resources', id: 'resources' },
]

<HeroPattern />

# API Documentation

Use the Protocol API to access contacts, conversations, group messages, and more and seamlessly integrate your product into the workflows of dozens of devoted Protocol users. {{ className: 'lead' }}

<div className="not-prose mt-6 mb-16 flex gap-3">
  <Button href="/quickstart" arrow="right">
    <>Quickstart</>
  </Button>
  <Button href="/sdks" variant="outline">
    <>Explore SDKs</>
  </Button>
</div>

## Getting started {{ anchor: false }}

To get started, create a new application in your [developer settings](#), then read about how to make requests for the resources you need to access using our HTTP APIs or dedicated client SDKs. When your integration is ready to go live, publish it to our [integrations directory](#) to reach the Protocol community. {{ className: 'lead' }}

<div className="not-prose">
  <Button href="/sdks" variant="text" arrow="right">
    <>Get your API key</>
  </Button>
</div>
