import { YouTubeEmbed } from '@next/third-parties/google';
export const metadata = {
  priority: 0,
  title: 'Timelapse Machine',
  description:
    'This robot is built for capturing motion time-lapses, where the camera moves while recording. The robot takes tiny steps over long periods, producing smooth, dynamic shots. Its path can be set to follow a straight line, trace a circle, or track along a tape on the ground. Developed in C++ with STM32 platform. Uses different components such as LCD, Steppers and infra sensors in GPIO and I2C. Graded 34.6/40, with a mean of 21.5, SD 7.9',
  descriptionMd:
    `This **robot** is built for capturing **motion time-lapses**, where the camera moves while recording. The robot takes tiny steps over long periods, producing smooth, dynamic shots. Its path can be set to follow a straight line, trace a circle, or track along a tape on the ground.
- Developed in **C++** with **STM32** platform
- Uses different components such as LCD, Steppers and infra sensors in GPIO and I²C
- Graded 34.6/40, with a mean of 21.5, SD 7.9`,
  summary: `Timelapse Machine is a robotics project built to capture motion time lapses, where the camera moves during recording rather than staying fixed. The page explains that the robot takes tiny, timed steps over long periods so the final footage looks smooth and dynamic, even for slow processes like plant growth. It supports multiple motion patterns, including straight line tracking, circular paths, and tape following, giving flexibility for different scenes. The project was developed in C++ on an STM32 platform and integrates typical embedded components such as an LCD, stepper motors, and infrared sensors connected over GPIO and I2C. A demo video showcases the device in action. The writeup is concise, focusing on the purpose, core behavior, and the hardware stack rather than a deep technical teardown. It also notes academic context and performance, including a strong grade relative to class mean, which frames the project as both a practical build and a successful course deliverable. Overall, the page is a compact overview of a motion control robot designed for creative time lapse capture, emphasizing the unique moving camera approach and the embedded systems skills involved.`,
};

# Timelapse Machine (ELEC 3300)

#robotics #embedded #stm32 

This robot is built for capturing time-lapses—long processes like plant growth condensed into short videos. Unlike a static time-lapse, it creates motion time-lapses, where the camera moves while recording. The robot takes tiny steps over long periods, producing smooth, dynamic shots. Its path can be set to follow a straight line, trace a circle, or track along a tape on the ground.

- Developed in C++ with STM32 platform
- Uses different components such as LCD, Steppers and infra sensors in GPIO and I²C
- Graded 34.6/40, with a mean of 21.5, SD 7.9

<YouTubeEmbed videoid="5U22TsrbzZY" />
