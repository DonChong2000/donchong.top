# donchong-top-db repo location

The `donchong-top-db` project has been moved out of this monorepo into its own standalone repository at:

- `/workspace/donchong-top-db`

This keeps the DB ingestion project independently versioned.
